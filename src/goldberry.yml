$schema: vscode://schemas/color-theme
name: Valinor Goldberry
author: Diego Brocanelli
maintainers:
- Diego Brocanelli <diegod2@msn.com>
semanticClass: theme.valinor.goldberry
type: light
semanticHighlighting: true
# colorSideBar: "#f2f0e5"
# colorBackground: "#f3f3f3"
# colorTheme: "#7ecbca"
# colorContrast "#7ecbca"
colors:
  # GENERAL
  focusBorder: "#7ecbca" # Cor da borda de foco em elementos interativos
  progressBar.background: "#7ecbca" # Cor de fundo da barra de progresso
  selection.background: "#7ecbca" # Cor de seleção global (listas, inputs, etc)

  # ACTIVITY BAR (barra lateral de ícones)
  activityBar.background: '#f2f0e5' # Fundo da barra de atividades (ícones laterais)
  activityBar.foreground: '#000' # Cor dos ícones na barra de atividades
  activityBarBadge.background: "#7ecbca" # Fundo do badge (notificações)
  activityBarBadge.foreground: '#fff' # Cor do texto do badge

  # SIDE BAR (explorer, search, etc)
  sideBar.background: "#f2f0e5" # Fundo da sidebar
  sideBar.foreground: '#000' # Texto padrão da sidebar
  sideBarTitle.foreground: '#000' # Título da sidebar
  sideBarSectionHeader.background: '#1f1f28' # Fundo dos headers de seção
  sideBarSectionHeader.foreground: '#000' # Texto dos headers de seção
  list.inactiveSelectionBackground: "#7ecbca" # Fundo de item selecionado sem foco
  list.inactiveSelectionForeground: '#000' # Texto de item selecionado sem foco
  list.activeSelectionBackground: '#7ecbca' # Fundo de item selecionado com foco
  list.activeSelectionForeground: '#000' # Texto de item selecionado com foco
  list.hoverBackground: '#7ecbca' # Fundo ao passar o mouse
  list.focusBackground: "#7ecbca" # Fundo quando focado via teclado
  list.highlightForeground: "#7ecbca" # Destaque de match (busca)
  list.focusHighlightForeground: '#f3f3f3' # Cor do highlight quando focado

  # STATUS BAR
  statusBar.background: "#7ecbca" # Fundo da status bar
  statusBar.foreground: '#fff' # Texto da status bar
  statusBar.noFolderBack: "#7ecbca" # Fundo quando nenhum projeto está aberto
  statusBar.remoteBackground: "#7ecbca" # Fundo quando conectado remotamente

  # TITLE BAR / MENUS
  titleBar.activeBackground: "#f2f0e5" # Fundo da barra de título ativa
  menu.background: "#f2f0e5" # Fundo dos menus dropdown
  menu.selectionBackground: "#7ecbca" # Fundo de item selecionado no menu

  # BUTTONS / LINKS
  button.background: "#7ecbca" # Fundo de botões
  button.foreground: '#fff' # Texto de botões
  button.hoverBackground: "#7ecbca" # Fundo ao passar o mouse no botão
  textLink.foreground: "#403ffff53" # Cor de links clicáveis
  editorLink.activeForeground: '#fff' # Cor de links ativos dentro do editor

  # INPUTS / FORMS
  input.background: '#f3f3f3' # Fundo de campos de input
  inputOption.activeBackground: "#7ecbca" # Fundo de opções selecionadas (checkbox/radio)
  inputOption.activeForeground: "#fff" # Texto de opções selecionadas
  quickInput.background: '#f2f0e5' # Fundo da palette (Ctrl+P, Ctrl+Shift+P)
  quickInput.foreground: '#000' # Texto da palette
  quickInputList.focusBackground: '#7ecbca' # Fundo do item focado na palette
  quickInputList.focusForeground: '#000' # Texto do item focado na palette

  # PANEL (terminal, output, problems)
  panel.background: '#f2f0e5' # Fundo do painel inferior (terminal, output)
  badge.background: "#7ecbca" # Fundo de badges
  badge.foreground: '#fff' # Texto de badges

  # TERMINAL
  terminal.background: '#f3f3f3' # Fundo do terminal
  terminal.foreground: '#000' # Texto do terminal
  terminalCursor.foreground: '#000' # Cor do cursor do terminal
  terminal.Cursor.background: "#7ecbca" # Cor de fundo do cursor (bloco)

  # TABS
  editorGroupHeader.tabsBackground: "#f2f0e5" # Fundo da área de tabs
  tab.activeBackground: "#f3f3f3" # Fundo da tab ativa
  tab.activeForeground: '#000' # Texto da tab ativa
  tab.inactiveBackground: '#f2f0e5' # Fundo da tab inativa
  tab.inactiveForeground: '#000' # Texto da tab inativa

  # BREADCRUMBS
  breadcrumb.background: "#f3f3f3" # Fundo do breadcrumb (caminho do arquivo)

  # WIDGETS (modais, popups, find)
  editorWidget.background: '#f2f0e5' # Fundo de widgets (ex: Ctrl+F)
  widget.border: '#f3f3f3' # Borda de widgets/modais
  pickerGroup.foreground: "#000" # Título de grupos na palette
  pickerGroup.border: '#403d52' # Borda separadora de grupos
  debugToolBar.background: "#f2f0e5" # Fundo da toolbar de debug

  # NOTIFICATIONS
  notification.background: "#f3f3f3" # Fundo das notificações

  # EDITOR
  editor.background: "#f3f3f3" # Fundo do editor
  editor.foreground: '#c5c9c5' # Texto padrão do editor
  editorCursor.foreground: '#c8c093' # Cor do cursor
  editor.lineHighlightBackground: '#f2f0e5' # Linha atual destacada
  editor.selectionBackground: '#7ecbca' # Fundo da seleção
  editor.selectionHighlightBackground: '#7ecbca' # Fundo de seleção secundária
  editor.wordHighlightBackground: '#2d4f6766' # Destaque de palavras iguais
  editorWhitespace.foreground: '#363646' # Caracteres invisíveis
  editorIndentGuide.background1: '#2d4f67' # Guias de indentação
  editorRuler.foreground: '#2d4f67' # Régua vertical
  editorLineNumber.foreground: '#2d4f67' # Números de linha
  editorLineNumber.activeForeground: '#c5c9c5' # Número da linha atual
tokenColors:
- name: Keywords & Storage – italic (valinor-patch)
  scope:
  - keyword.control
  - keyword.operator.expression
  - keyword.operator.new
  - keyword.control.new
  - storage.type
  - storage.modifier
  - keyword.other.use
  - keyword.other.namespace
  - storage.modifier.extends
  - entity.other.inherited-class
  - variable.language
  - variable.language.this
  - variable.language.self
  - variable.language.super
  - variable.language.parent
  - variable.language punctuation.definition.variable.php
  - punctuation.definition.variable.php
  - keyword.control.php
  - keyword.control.flow
  - keyword.control.async-modifier
  - keyword.control.import
  - keyword.control.export
  - keyword.control.from
  - keyword.control.return
  settings:
    fontStyle: italic
- scope:
  - comment
  - punctuation.definition.comment
  settings:
    foreground: '#b7b5ac'
    fontStyle: italic
- scope:
  - keyword
  - keyword.control
  - keyword.operator.expression
  settings:
    foreground: '#8b7ec8'
- scope:
  - storage
  - storage.type
  - storage.modifier
  settings:
    foreground: '#205ea6'
- scope:
  - variable
  settings:
    foreground: '#7ecbca'
- scope:
  - variable.language
  settings:
    foreground: '#af3029'
    fontStyle: italic
- scope:
  - string
  - string.quoted
  - punctuation.definition.string
  settings:
    foreground: '#66800b'
- scope:
  - constant
  - constant.numeric
  - constant.language
  settings:
    foreground: '#ad8301'
- scope:
  - entity.name.function
  - support.function
  settings:
    foreground: '#205ea6'
- scope:
  - entity.name.type.class
  - entity.name.class
  - support.class
  settings:
    foreground: '#24837b'
- scope:
  - entity.other.inherited-class
  settings:
    foreground: '#24837b'
    fontStyle: italic
- scope:
  - entity.name.tag
  settings:
    foreground: '#af3029'
- scope:
  - entity.other.attribute-name
  settings:
    foreground: '#24837b'
    fontStyle: italic
- scope:
  - support.type
  - support.class
  settings:
    foreground: '#205ea6'
- name: Function parameters
  scope:
  - variable.parameter
  - entity.name.variable.parameter
  settings:
    fontStyle: italic
- name: Keyword new
  scope:
  - keyword.control.new
  - keyword.operator.new
  settings:
    fontStyle: bold italic
- name: Markup italic
  scope:
  - markup.italic
  settings:
    fontStyle: italic
- name: Markup bold
  scope:
  - markup.bold
  settings:
    fontStyle: bold
- name: Markup heading
  scope:
  - markup.heading
  settings:
    foreground: '#205ea6'
    fontStyle: bold
