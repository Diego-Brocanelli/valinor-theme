$schema: vscode://schemas/color-theme
name: Valinor Feanor
author: Diego Brocanelli
maintainers:
- Diego Brocanelli <diegod2@msn.com>
semanticClass: theme.valinor.fangorn
type: dark
semanticHighlighting: true
# colorSideBar: "#eaeaea"
# colorBackground: "#f5f5f5"
# colorTheme: "#d6cfe2"
# colorContrast "#d6cfe252"
colors:
  # GENERAL
  focusBorder: "#d6cfe252" # Cor da borda de foco em elementos interativos
  progressBar.background: "#d6cfe252" # Cor de fundo da barra de progresso
  selection.background: "#d6cfe252" # Cor de seleção global (listas, inputs, etc)

  # ACTIVITY BAR (barra lateral de ícones)
  activityBar.background: '#eaeaea' # Fundo da barra de atividades (ícones laterais)
  activityBar.foreground: '#000' # Cor dos ícones na barra de atividades
  activityBarBadge.background: "#d6cfe2" # Fundo do badge (notificações)
  activityBarBadge.foreground: '#000' # Cor do texto do badge

  # SIDE BAR (explorer, search, etc)
  sideBar.background: "#eaeaea" # Fundo da sidebar
  sideBar.foreground: '#000' # Texto padrão da sidebar
  sideBarTitle.foreground: '#000' # Título da sidebar
  sideBarSectionHeader.background: '#1f1f28' # Fundo dos headers de seção
  sideBarSectionHeader.foreground: '#000' # Texto dos headers de seção
  list.inactiveSelectionBackground: "#d6cfe252" # Fundo de item selecionado sem foco
  list.inactiveSelectionForeground: '#000' # Texto de item selecionado sem foco
  list.activeSelectionBackground: '#d6cfe252' # Fundo de item selecionado com foco
  list.activeSelectionForeground: '#000' # Texto de item selecionado com foco
  list.hoverBackground: '#d6cfe252' # Fundo ao passar o mouse
  list.focusBackground: "#d6cfe252" # Fundo quando focado via teclado
  list.highlightForeground: "#d6cfe2" # Destaque de match (busca)
  list.focusHighlightForeground: '#f5f5f5' # Cor do highlight quando focado

  # STATUS BAR
  statusBar.background: "#d6cfe2" # Fundo da status bar
  statusBar.foreground: '#000' # Texto da status bar
  statusBar.noFolderBack: "#d6cfe252" # Fundo quando nenhum projeto está aberto
  statusBar.remoteBackground: "#d6cfe2" # Fundo quando conectado remotamente

  # TITLE BAR / MENUS
  titleBar.activeBackground: "#eaeaea" # Fundo da barra de título ativa
  menu.background: "#eaeaea" # Fundo dos menus dropdown
  menu.selectionBackground: "#d6cfe252" # Fundo de item selecionado no menu

  # BUTTONS / LINKS
  button.background: "#d6cfe2" # Fundo de botões
  button.foreground: '#000' # Texto de botões
  button.hoverBackground: "#d6cfe252" # Fundo ao passar o mouse no botão
  textLink.foreground: "#000" # Cor de links clicáveis
  editorLink.activeForeground: '#d6cfe2' # Cor de links ativos dentro do editor

  # INPUTS / FORMS
  input.background: '#f5f5f5' # Fundo de campos de input
  inputOption.activeBackground: "#d6cfe2" # Fundo de opções selecionadas (checkbox/radio)
  inputOption.activeForeground: "#000" # Texto de opções selecionadas
  quickInput.background: '#eaeaea' # Fundo da palette (Ctrl+P, Ctrl+Shift+P)
  quickInput.foreground: '#000' # Texto da palette
  quickInputList.focusBackground: '#d6cfe2' # Fundo do item focado na palette
  quickInputList.focusForeground: '#000' # Texto do item focado na palette

  # PANEL (terminal, output, problems)
  panel.background: '#eaeaea' # Fundo do painel inferior (terminal, output)
  badge.background: "#d6cfe2" # Fundo de badges
  badge.foreground: '#000' # Texto de badges

  # TERMINAL
  terminal.background: '#f5f5f5' # Fundo do terminal
  terminal.foreground: '#ooo' # Texto do terminal
  terminalCursor.foreground: '#ooo' # Cor do cursor do terminal
  terminal.Cursor.background: "#d6cfe2" # Cor de fundo do cursor (bloco)

  # TABS
  editorGroupHeader.tabsBackground: "#eaeaea" # Fundo da área de tabs
  tab.activeBackground: "#f5f5f5" # Fundo da tab ativa
  tab.activeForeground: '#000' # Texto da tab ativa
  tab.inactiveBackground: '#eaeaea' # Fundo da tab inativa
  tab.inactiveForeground: '#000' # Texto da tab inativa

  # BREADCRUMBS
  breadcrumb.background: "#f5f5f5" # Fundo do breadcrumb (caminho do arquivo)

  # WIDGETS (modais, popups, find)
  editorWidget.background: '#eaeaea' # Fundo de widgets (ex: Ctrl+F)
  widget.border: '#f5f5f5' # Borda de widgets/modais
  pickerGroup.foreground: "#d6cfe2" # Título de grupos na palette
  pickerGroup.border: '#403d52' # Borda separadora de grupos
  debugToolBar.background: "#eaeaea" # Fundo da toolbar de debug

  # NOTIFICATIONS
  notification.background: "#f5f5f5" # Fundo das notificações

  # EDITOR
  editor.background: "#f5f5f5" # Fundo do editor
  editor.foreground: '#c5c9c5' # Texto padrão do editor
  editorCursor.foreground: '#c8c093' # Cor do cursor
  editor.lineHighlightBackground: '#eaeaea' # Linha atual destacada
  editor.selectionBackground: '#d6cfe252' # Fundo da seleção
  editor.selectionHighlightBackground: '#d6cfe252' # Fundo de seleção secundária
  editor.wordHighlightBackground: '#2d4f6766' # Destaque de palavras iguais
  editorWhitespace.foreground: '#363646' # Caracteres invisíveis
  editorIndentGuide.background1: '#2d4f67' # Guias de indentação
  editorRuler.foreground: '#2d4f67' # Régua vertical
  editorLineNumber.foreground: '#2d4f67' # Números de linha
  editorLineNumber.activeForeground: '#c5c9c5' # Número da linha atual
tokenColors:
- name: Keywords & Storage – italic (valinor-patch)
  scope:
  - keyword.control
  - keyword.operator.expression
  - keyword.operator.new
  - keyword.control.new
  - storage.type
  - storage.modifier
  - keyword.other.use
  - keyword.other.namespace
  - storage.modifier.extends
  - entity.other.inherited-class
  - variable.language
  - variable.language.this
  - variable.language.self
  - variable.language.super
  - variable.language.parent
  - variable.language punctuation.definition.variable.php
  - punctuation.definition.variable.php
  - keyword.control.php
  - keyword.control.flow
  - keyword.control.async-modifier
  - keyword.control.import
  - keyword.control.export
  - keyword.control.from
  - keyword.control.return
  settings:
    fontStyle: italic
- scope:
  - comment
  - punctuation.definition.comment
  settings:
    foreground: '#7a8478'
    fontStyle: italic
- scope:
  - keyword
  - keyword.control
  - keyword.operator.expression
  settings:
    foreground: '#e67e80'
- scope:
  - storage
  - storage.type
  - storage.modifier
  settings:
    foreground: '#d699b6'
- scope:
  - variable
  settings:
    foreground: '#d6cfe2'
- scope:
  - variable.language
  settings:
    foreground: '#e69875'
    fontStyle: italic
- scope:
  - string
  - string.quoted
  - punctuation.definition.string
  settings:
    foreground: '#d6cfe2'
- scope:
  - constant
  - constant.numeric
  - constant.language
  settings:
    foreground: '#dbbc7f'
- scope:
  - entity.name.function
  - support.function
  settings:
    foreground: '#7fbbb3'
- scope:
  - entity.name.type.class
  - entity.name.class
  - support.class
  settings:
    foreground: '#d699b6'
- scope:
  - entity.other.inherited-class
  settings:
    foreground: '#d699b6'
    fontStyle: italic
- scope:
  - entity.name.tag
  settings:
    foreground: '#e67e80'
- scope:
  - entity.other.attribute-name
  settings:
    foreground: '#83c092'
    fontStyle: italic
- scope:
  - support.type
  - support.class
  settings:
    foreground: '#7fbbb3'
- name: Function parameters
  scope:
  - variable.parameter
  - entity.name.variable.parameter
  settings:
    fontStyle: italic
- name: Keyword new
  scope:
  - keyword.control.new
  - keyword.operator.new
  settings:
    fontStyle: bold italic
- name: Markup italic
  scope:
  - markup.italic
  settings:
    fontStyle: italic
- name: Markup bold
  scope:
  - markup.bold
  settings:
    fontStyle: bold
- name: Markup heading
  scope:
  - markup.heading
  settings:
    foreground: '#7fbbb3'
    fontStyle: bold
